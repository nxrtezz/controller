services:
  web:
    build: .
    ports:
      - "8417:8417"
    environment:
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY:-change-me-before-production}
      DJANGO_DEBUG: "0"
      # Default covers this machine and its current LAN address; set a trusted-host list in .env for production.
      DJANGO_ALLOWED_HOSTS: ${DJANGO_ALLOWED_HOSTS:-localhost,127.0.0.1,192.168.1.106}
      DATABASE_PATH: /data/controller.sqlite3
    volumes:
      - controller_data:/data
    command: >
      sh -c "python manage.py migrate --noinput &&
      python manage.py collectstatic --noinput &&
      gunicorn controller.wsgi:application --bind 0.0.0.0:8417 --workers 3"
    restart: unless-stopped

  monitor:
    build: .
    environment:
      DJANGO_SECRET_KEY: ${DJANGO_SECRET_KEY:-change-me-before-production}
      DJANGO_DEBUG: "0"
      DATABASE_PATH: /data/controller.sqlite3
    volumes:
      - controller_data:/data
    command: sh -c "python manage.py migrate --noinput && python manage.py monitor_bustimes"
    restart: unless-stopped
    depends_on:
      - web

volumes:
  controller_data:
